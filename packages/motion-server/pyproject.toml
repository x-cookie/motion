[project]
name = "motion-server"
version = "0.18.5"
description = "FastAPI server for Motion task management system"
readme = "README.md"
requires-python = ">=3.12"
authors = [
    { name = "Kohei Wada" }
]
license = { text = "MIT" }
keywords = ["task", "management", "api", "fastapi"]
classifiers = [
    "Development Status :: 4 - Beta",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
]

dependencies = [
    "motion-core==0.18.5",
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "pydantic>=2.10.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "pytest-cov>=4.0.0",
    "pytest-asyncio>=1.0.0",
    "httpx>=0.27.0",  # For testing API endpoints
    "mypy>=1.0.0",
    "ruff>=0.7.0",
]

[project.scripts]
motion-server = "motion_server.main:main"

[project.urls]
Homepage = "https://github.com/Kohei-Wada/taskdog"
Repository = "https://github.com/Kohei-Wada/taskdog"
"Bug Tracker" = "https://github.com/Kohei-Wada/taskdog/issues"

[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = [
    "motion_server",
    "motion_server.api",
    "motion_server.api.models",
    "motion_server.api.routers",
    "motion_server.config",
    "motion_server.websocket",
    "motion_server.infrastructure",
    "motion_server.infrastructure.logging",
]
package-dir = { "" = "src" }

[tool.setuptools.package-data]
motion_server = ["py.typed"]

[tool.mypy]
python_version = "3.13"
strict = true
# Disable unused-ignore warnings (environment-dependent)
warn_unused_ignores = false

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
asyncio_mode = "auto"
filterwarnings = [
    "ignore::ResourceWarning:holidays.*",
]
