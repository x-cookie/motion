/* Shared Component Styles */

/* Dialog Components */
#dialog-title {
    text-align: center;
    padding: 0;
    background: transparent;
}

#button-container {
    height: auto;
    align: center middle;
    margin-top: 1;
}

#button-container Button {
    margin: 0 1;
}

/* Form Components */
Input {
    margin: 0;
}
