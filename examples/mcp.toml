# MCP Server Configuration
# Copy to: ~/.config/motion/mcp.toml
#
# This configures the motion MCP server for Claude Desktop integration.
# All settings can also be overridden via environment variables.

# =============================================================================
# API Connection Settings
# =============================================================================
# Configure how the MCP server connects to the motion API server.

[api]
# API server hostname
# Environment variable: MOTION_API_HOST
host = "127.0.0.1"

# API server port
# Environment variable: MOTION_API_PORT
port = 8000

# API key for authentication (REQUIRED)
# The motion-server requires API key authentication by default.
# Generate a key using: python -c "import secrets; print(f'sk-{secrets.token_hex(24)}')"
# This key must match one configured in server.toml's [[auth.api_keys]] section.
# Environment variable: MOTION_API_KEY
# api_key = ""  # Set via MOTION_API_KEY environment variable (recommended)

# =============================================================================
# MCP Server Settings
# =============================================================================
# Configure the MCP server behavior.

[server]
# Server name shown to MCP clients (Claude Desktop)
# Environment variable: MOTION_MCP_NAME
name = "motion"

# Logging level (DEBUG, INFO, WARNING, ERROR)
# Environment variable: MOTION_MCP_LOG_LEVEL
log_level = "INFO"
